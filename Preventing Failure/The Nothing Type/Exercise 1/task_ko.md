## `Nothing` 타입 (#1)

`msg: String` 파라미터를 받아 이를 기본 클래스 생성자에 전달하는 예외 클래스 `Failure`를 만드세요. `msg`를 `trace()`에 전달한 뒤 `Failure(msg)`를 던지는 함수 `fail(msg: String)`을 작성하세요.

이제 `fail()`을 사용하는 `require()`와 `check()`의 버전을 직접 작성하세요 ([Check Instructions]에서 제공된 것을 참조).  
`main()`에 있는 시작 코드가 당신의 함수를 테스트합니다.

<sub> 이 작업에는 자동 테스트가 포함되어 있지 않아  
"Check"를 실행하면 항상 "Correct"로 표시됩니다.  
제공된 솔루션과 비교하며 확인해 주세요! </sub>